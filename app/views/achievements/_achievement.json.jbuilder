json.extract! achievement, :id, :player_name, :sport_name, :achievement, :captain, :gender, :created_at, :updated_at
json.url achievement_url(achievement, format: :json)

# json.extract! post, :id, :sport_name, :title, :image, :description, :tag, :user_id, :created_at, :updated_at
# json.url post_url(post, format: :json)
