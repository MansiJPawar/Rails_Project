<%= form_with(model: [@sport, announcement], local: true) do |form| %>
  <% if announcement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(announcement.errors.count, "error") %> prohibited this announcement from being saved:</h2>

      <ul>
      <% announcement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.text_field :announcement_title, class:"form-control" , placeholder:"announcement title"%>
  </div>

  <div class="field form-group">
    <%= form.text_field :content,class:"form-control" , placeholder:"content" %>
  </div>
 
  <%# <div class="field form-group">
    <%= form.collection_select(:sport_id, Sport.all, :id, :sport_name, { :prompt => 'Select a Sport', :selected => @announcement.sport_id }, { class: 'form-control' }) </div>%>
  

  <div class="actions form-group">
    <%= form.submit class:"btn btn-light" %>
  </div>
<% end %>
